# 应用配置
PROJECT_NAME="教务管理系统"
API_V1_STR="/api/v1"
SECRET_KEY="change_this_to_a_secret_key"
ACCESS_TOKEN_EXPIRE_MINUTES=11520  # 60分钟 * 24小时 * 8天 = 8天
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080", "http://localhost"]

# 环境配置
ENVIRONMENT="production"  # production, development, testing
DEBUG=False

# 日志配置
LOG_LEVEL="INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_DIR="./logs"  # 日志目录
LOG_ROTATION="00:00"  # 每天午夜轮转
LOG_RETENTION="30 days"  # 保留30天

# 数据库配置
DB_HOST="localhost"
DB_USER="postgres"
DB_PASSWORD="postgres"
DB_NAME="edu_db"
DB_PORT="5432"

# Redis配置
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=""  # 如果有密码，请设置

# 邮箱配置（可选）
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST=""
SMTP_USER=""
SMTP_PASSWORD=""
EMAILS_FROM_EMAIL=""
EMAILS_FROM_NAME="教务管理系统" 